{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare yarn@4.6.0 --activate && corepack yarn install",
  "buildCommand": "corepack yarn workspace @sporthub/shared build && corepack yarn workspace @sporthub/web build",
  "outputDirectory": "apps/web/.next"
}
