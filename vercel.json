{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "corepack enable && yarn install",
  "buildCommand": "yarn workspace @sporthub/shared build && yarn workspace @sporthub/web build",
  "outputDirectory": "apps/web/.next"
}
